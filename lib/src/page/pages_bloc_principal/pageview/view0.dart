import 'package:flutter/material.dart';

class ViewPage0 extends StatelessWidget {
  const ViewPage0({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('sewimprime den euvo');
    return const Scaffold(
      backgroundColor: Colors.red,
      body: Center(
        child: Text('Página 0'),
      ),
    );
  }
}
